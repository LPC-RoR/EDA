<%= form_with(model: tabla, local: true) do |form| %>
  <% if tabla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tabla.errors.count, "error") %> prohibited this tabla from being saved:</h2>

      <ul>
      <% tabla.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tabla %>
    <%= form.text_field :tabla %>
  </div>

  <div class="field">
    <%= form.label :padre_id %>
    <%= form.number_field :padre_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
