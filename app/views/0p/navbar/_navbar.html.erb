<!-- IMAGEN -->
<!--%= image_tag 'logo.jpg' %-->

<!-- MENU -->
<nav class="navbar navbar-expand-sm navbar-light">
  <ul class="navbar-nav">

    <!-- Controladores que no tienen menú -->
    <% unless nomenu?(controller_name) %>
      <li>
        <!-- USUARIO AUTENTICADO -->
          <% Recurso::MENU.each do |item| %>
            <%= render partial: '0p/navbar/btn', locals: {nombre: item[0].capitalize, link: item[1], delete: false} %>
          <% end %>
        <% if usuario_signed_in? %>


          <!-- EMPLEADOS: acceso sólo a usuarios tipo ['sistema', 'planta'] -->
          <!--% if (['sistema', 'planta'].include? Current.empleado.tipo) %-->
            <!--% Recurso::MENU.each do |item| %-->
              <!--%= render partial: 'apariencia/btn_navbar', locals: {nombre: item[:item].capitalize, link: item[:link], delete: false} %-->
            <!--% end %-->
          <!--% end %-->


          <!-- CUALQUIER USUARIO puede salir de la sesión -->
          <%= render partial: '0p/navbar/btn', locals: {nombre: 'Salir', link: destroy_usuario_session_path, delete: true} %>
        <!-- USUARIO NO AUTENTICADO -->
        <% else %>
          <%= render partial: '0p/navbar/btn', locals: {nombre: 'Ingreso', link: new_usuario_session_path, delete: false} %>
        <% end %>
      </li>
    <% end %>
  </ul>
</nav>
