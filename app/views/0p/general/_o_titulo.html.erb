<!-- TITULO -->
<!-- size: determina el tamaño del font -->
<!-- titulo: Es el texto del título -->
<h<%= size%>>
	<% case a %>
	<% when 'show' %>
		<%= "! #{o.class.name} : #{show_title(o)}" %>
	<% else %>
		<%= titulo %>
	<% end %>
</h<%= size%>>
