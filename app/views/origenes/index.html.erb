<p id="notice"><%= notice %></p>

<h1>Origenes</h1>

<table>
  <thead>
    <tr>
      <th>Repositorio</th>
      <th>Publicacion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @origenes.each do |origen| %>
      <tr>
        <td><%= origen.repositorio_id %></td>
        <td><%= origen.publicacion_id %></td>
        <td><%= link_to 'Show', origen %></td>
        <td><%= link_to 'Edit', edit_origen_path(origen) %></td>
        <td><%= link_to 'Destroy', origen, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Origen', new_origen_path %>
