<p id="notice"><%= notice %></p>

<p>
  <strong>Tema:</strong>
  <%= @herencia.tema_id %>
</p>

<p>
  <strong>Proyecto:</strong>
  <%= @herencia.proyecto_id %>
</p>

<%= link_to 'Edit', edit_herencia_path(@herencia) %> |
<%= link_to 'Back', herencias_path %>
