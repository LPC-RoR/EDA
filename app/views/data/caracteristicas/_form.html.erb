<%= form_with(model: caracteristica, local: true) do |form| %>
  <% if caracteristica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caracteristica.errors.count, "error") %> prohibited this caracteristica from being saved:</h2>

      <ul>
      <% caracteristica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :caracteristica %>
    <%= form.text_field :caracteristica %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="field">
    <%= form.label :caracterizacion_id %>
    <%= form.number_field :caracterizacion_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
