<%= form_with(model: caracterizacion, local: true) do |form| %>
  <% if caracterizacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caracterizacion.errors.count, "error") %> prohibited this caracterizacion from being saved:</h2>

      <ul>
      <% caracterizacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :caracterizacion %>
    <%= form.text_field :caracterizacion %>
  </div>

  <div class="field">
    <%= form.label :proyecto_id %>
    <%= form.number_field :proyecto_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
