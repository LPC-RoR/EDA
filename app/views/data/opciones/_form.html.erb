<%= form_with(model: opcion, local: true) do |form| %>
  <% if opcion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opcion.errors.count, "error") %> prohibited this opcion from being saved:</h2>

      <ul>
      <% opcion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :opcion %>
    <%= form.text_field :opcion %>
  </div>

  <div class="field">
    <%= form.label :caracteristica_id %>
    <%= form.number_field :caracteristica_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
