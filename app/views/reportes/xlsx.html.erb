<table class="table table-striped table-sm  thead-dark">
  <thead>
    <th>Author</th>
    <th>Title</th>
    <th>Folders</th>
  </thead>

  <tbody>
  	<!-- Opción para evitar escribir el parcial _modelo.html.erb en cada controlador-->
  	<% unless @coleccion['publicaciones'].blank? %>
	  	<% @coleccion['publicaciones'].each do |publicacion| %>
        <tr>
          <td><%= publicacion.author %></td>
          <td><%= publicacion.title %></td>
          <td><%= publicacion.folders %></td>
        </tr>
  		<% end  %>
  	<% end %>
  </tbody>
</table>
