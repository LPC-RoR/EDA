<%= form_with(model: linea, local: true) do |form| %>
  <% if linea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(linea.errors.count, "error") %> prohibited this linea from being saved:</h2>

      <ul>
      <% linea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orden %>
    <%= form.number_field :orden %>
  </div>

  <div class="field">
    <%= form.label :tabla_id %>
    <%= form.number_field :tabla_id %>
  </div>

  <div class="field">
    <%= form.label :linea %>
    <%= form.text_field :linea %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
